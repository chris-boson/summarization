from setuptools import find_packages, setup

setup(
    name="summarization",
    version='0.0.1',
    author="",
    description="dummy",
    packages=find_packages(),
    install_requires=[
        "pytorch-lightning"
    ],
)
